;; BOCAL STAFF 2010-2011
;; FEDORA DUMP

(load "std.el")
(load "std_comment.el")
(load "tuareg-site-file.el")
(if (file-exists-p "~/.myemacs") 
    (load-file "~/.myemacs"))
; EOF
(put 'upcase-region 'disabled nil)
